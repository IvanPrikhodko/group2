{
  "name": "group2project-frontend",
  "version": "0.0.0",
  "engines": {
    "node": "20.2.0",
    "npm": "9.6.6"
  },
  "scripts": {
    "ng": "ng",
    "start": "node server.js",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test",
    "heroku-postbuild": "ng build"
  }
}
